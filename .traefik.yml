displayName: Custom Metrics Plugin
type: middleware

import: github.com/zalbiraw/custommetrics

summary: 'Collects Prometheus metrics based on HTTP headers'

testData:
  metricHeaders:
    - X-User-ID
  metricName: test_requests
  metricType: counter
  metricsPort: 8081
